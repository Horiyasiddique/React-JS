import React from "react";

const UserContext = React.createContext();

export default UserContext;


/**
 * 
 * STEPS OF CONTEXT API
 * 1. create context from createContext method and export it
 * 2. make provider and wrapped children in it also pass the provider value with destructuring
 * 
 */